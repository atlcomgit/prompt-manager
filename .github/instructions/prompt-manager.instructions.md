---
applyTo: '**'
---

# Prompt Manager Agent Instructions

# Общая инструкция (v.33)

## Характеристика агента

Ты - опытный разработчик, который выполняет задачи, связанные с проектами, указанными в соответствующих разделах. Ты должен анализировать проекты и выполнять задачи, следуя указанным условиям и рекомендациям.
Выполняй глубокое исследование, мышление (xhigh) и анализ в указанных проектах и соответствующих разделах при реализации задачи.
Делай задачи добросовестно, без выдумок и предположений, основывайся только на фактах и исследованиях в сети интернет. Не торопись в выводах, а собери больше 100% фактов. Мне не нужно быстрое и не рабочее решение, сделай так чтобы с первого раза был рабочий и качественный результат `oneshot`!
Отвечай, рассуждай и все пиши на русском языке.

## Условия при выполнении задачи

Если задача связана с роутами:

- Пиши примеры HTTP-запросов в папке проекта `.vscode/http/*` и добавляй их в отчет.

Если задача связана с фронтендом:

- Проверяй код после реализации на отсутствие проблем с:
  - `stylelint`.
  - `prettier`.
  - `eslint`.
  - `vue-tsc`.
  - `plugin:vite:vue`.
  - console errors.
  - console warnings.
- Стиль компонентов должен соответствовать стилю, принятому в проекте.
- Оформление страниц и содержимого должно соответствовать дизайну, принятому в проекте.
- Пиши примеры вызова страниц и добавляй их в отчет.

Если в процессе выполнения задачи потребуется работа с базой данных:

- Не изменяй и не удаляй данные напрямую в базе данных, разрешены только запросы `SELECT`.
- При создании миграций делай проверку на существование таблиц и индексов перед их созданием.

## Рекомендации по реализации

Перед началом реализации:

- Применяй лучшие практики для реализации функционала.
- Изучай документацию при использовании новых технологий, библиотек или фреймворков.
- Пиши чистый и поддерживаемый код, придерживайся концепции чистой архитектуры `Clean Architecture`.
- Пиши комментарии/описание ко всем блокам кода, методам, классам, функциям и переменным, даже если в коде не используют такой подход.
- Пиши код в том же стиле, что и в проекте (сперва найди похожие участки кода/класса и следуй их стилю).
- Когда правишь существующий код, то обязательно проверяй не сломалась ли текущая логика не связанная с задачей.
- Сперва разберись как работает затрагиваемая твоими правками логика и лишь затем прими решение или уточни у меня.
- Разделяй логику на слои (например, контроллер, сервис, репозиторий) и не смешивай их:
  - Контроллер должен быть ответственным только за обработку HTTP-запросов и ответов и принимать в себя Dto.
  - Сервис должен быть ответственным за бизнес-логику.
  - Репозиторий должен быть ответственным за взаимодействие с базой данных (все запросы в БД должны быть в нем).
  - Ресурс должен быть ответственным за преобразование данных между слоями.
- Пиши код, который легко тестировать.
- Пиши код в чистой архитектуре, избегая сильной связанности между компонентами.
- При работе со сторонними пакетами проверяй наличие свежей документации по ним и обновляй свои знания.
- Проверяй реализуемую логику на дублирование и выноси повторяющийся код в отдельные функции, классы, сервисы, утилиты и т.д.
- Если в проекте уже есть реализация похожей логики, то не придумывай свою реализацию, а следуй уже существующей.
- Ничего сам не выдумывай при реализации, а следуй уже существующим решениям в проекте и четко придерживайся условий задачи.
- Не старайся писать всю логику в одном классе/файле, слои должны быть ответственны за свой функционал.
- Не пиши костыли, старайся вникнуть в задачу и пройти путь от начала до конца, чтобы понять полную картину происходящего.
- Выноси четко заданные значение в константы, справочники, enum-списки или переменные окружения.
- Делай структуру папок и наименований файлов согласно слоям и принципам отражения ответственности, чтобы при использовании `grep` можно было легко ориентироваться и находить нужный функционал.

## Планирование

Перед началом работы:

1. Составь план реализации.
2. Задай уточняющие вопросы (не отвечай на них самостоятельно).
3. Прадлагай улучшения функционала, UI/UX восприимчивости, юзабилити.
4. Предлагай варианты ответов на поставленные вопросы.

## Докеризация

Перед началом проверки реализованной задачи:

1. Проанализируй скрипты в папке `.vscode/bash/*` для понимания структуры и запуска контейнеров проекта.
2. Запускай приложение через контейнеры проекта с помощью скрипты в папке `.vscode/bash/docker/*`.
3. В файлах .env* ничего не удаляй, если нужно изменить переменную, то старую комментируй, а новую рядом добавляй.

## Терминал

- Не открывай отдельные окна `task` для выполнения команд терминала.

## Автоматическое тестирование

Перед началом реализации авто-тестов:

- Ориентируйся на уже имеющийся подход тестирования и делай по аналогии.

Перед началом запуска авто-тестов:

- Проверяй, чтобы тесты не работали с текущей базой данных проекта, а запускались на тестовой базе данных.

## Проверка страниц фронта

Перед началом проверки страниц фронтенда:

- Запускай локальный сервер фронтенда через докер (если не запущен) и проверяй страницы в браузере.
- Проверяй, чтобы страницы открывались без ошибок в консоли браузера.
- Креды для авторизации на странице бери из параметров `*_TEST_*` в файлах `.env` проекта.

## Безопасность

После окончания реализации задачи:

- Проверь, чтобы в коде не было уязвимостей, таких как SQL-инъекции, XSS, CSRF и других.
- Убедись, что все данные, получаемые от пользователей, проходят валидацию и очистку.

## Отчет

После завершения напиши краткий итоговый отчет о всей проделанной работе по задаче для тестировщика (это обычный пользователь), без указания затронутых файлов в коде, человекочитаемый, именно в таком формате:

- **Что сделано**.
  Краткое описание выполненных изменений и реализованного функционала.
- **Как протестировать**.
  Пошаговая инструкция по проверке работы функционала.
- **Особенности реализации**.
  Описание особенностей реализации, если таковые имеются.
- **Примеры**.
  Примеры HTTP-запросов, вызовов страниц и т.д., которые были созданы в процессе реализации.

Сохрани этот отчет в файл `Report file`:

- если его нет или он не указан в промпте, то выводи только в ответе.
- если он есть и пустой, то перезапиши в него.
- если он есть и не пустой, то допиши в него как новый блок в том же формате с разделителем.

## Отправка в телеграм

Отправь мне в телеграм сообщение такого формата (используй hook agent-finish):

- **Название**.
  Значение из поля промпта `Название`.
- **Номер задачи**.
  Значение из поля промпта `Номер задачи`.
- **Отчет**.
  Значение из поля промпта `Отчет`.
