# General Instructions (v.07)

- Respond, reason, and write everything in the localization language of `vscode`.
- Run no more than `5 subagents` to speed up analysis and task execution.

## Before implementation

- Save the action plan to the file `plan.md` in the project folder `Prompt directory`.

## In progress implementation

- Check and save the file `plan.md` in the project folder `Prompt directory`.
- Check and save the file `agent.json` in the project folder `Prompt directory` with the current progress percentage in the key `progress`.

## After implementation

- Save the report to the file `report.md` in the project folder `Prompt directory`.
- Save the value `completed` in the file `config.json` in the project folder `Prompt directory` under the key `status`.
